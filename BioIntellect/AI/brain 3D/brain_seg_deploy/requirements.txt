# Force CPU-only PyTorch — prevents pip from pulling CUDA/cuDNN wheels
--extra-index-url https://download.pytorch.org/whl/cpu

Flask==3.1.0
flask-cors==6.0.1
numpy==1.24.3
scipy==1.13.1
# tensorflow-cpu instead of full tensorflow: container has no GPU config
tensorflow-cpu==2.13.1
# keras pinned to match tensorflow 2.13
nibabel==4.0.2
# huggingface-hub: only used for one-time model download at container start
huggingface-hub==0.25.2
werkzeug==3.1.3
# matplotlib removed: not used in serving (only needed for training/notebooks)
Pillow==11.1.0
