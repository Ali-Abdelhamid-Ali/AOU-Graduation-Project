# BioIntellect Backend Requirements

# Web Framework
fastapi==0.115.2
uvicorn==0.32.0
gunicorn==23.0.0
python-multipart==0.0.12
# starlette is a sub-dependency of fastapi — do not pin separately

# ML/DL Stack (CPU-only)
tensorflow-cpu==2.18.0
tf_keras==2.18.0
# keras==3.7.0 removed: conflicts with tensorflow-cpu 2.18 which bundles Keras 2
# scikit-learn removed: not directly used in backend code
numpy==1.26.4
scipy==1.14.1
PyWavelets==1.7.0

# Transformers stack for local LLMs (PhiQA / MedMO)
transformers==4.46.3
accelerate==1.13.0
sentencepiece==0.2.1
safetensors==0.4.5
qwen-vl-utils==0.0.14
pillow==11.0.0

# Medical Imaging
nibabel==5.3.2

# huggingface_hub: sub-dependency of transformers — do not pin separately
# protobuf: sub-dependency of qdrant-client — do not pin separately

# Utilities
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
email_validator==2.2.0
pypdf==5.1.0
aiofiles==24.1.0
# python-jose and passlib removed: not used (auth is handled by Supabase tokens)
langchain-community==0.3.7
langchain-text-splitters==0.3.2
langchain-core==0.3.18

# Database & Vector Store
supabase==2.10.0
qdrant-client==1.12.1

# Logging & Monitoring
psutil==6.1.0

# HTTP Client
httpx==0.27.2

# API Documentation
scalar_fastapi==1.8.1

# Rate Limiting & Caching
redis==5.2.0
slowapi==0.1.9

# Cloud provider SDKs
cohere==5.11.4
openai==1.54.4
